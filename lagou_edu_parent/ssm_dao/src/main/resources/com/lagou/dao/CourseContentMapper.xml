<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lagou.dao.CourseContentMapper">
    
    <!--因为CourseSection类中有一对多关系,所以建立一个映射-->
    <resultMap id="sectionResultMap" type="courseSection">
        <id property="id" column="id"></id>
        <result property="courseId" column="course_id"></result>
        <result property="sectionName" column="section_name"></result>
        <result property="description" column="description"></result>
        <result property="orderNum" column="order_num"></result>
        <result property="status" column="status"></result>
        
        <!-- 使用collection,配置一对多关系 -->
        <collection property="lessonList" ofType="courseLesson">
            <id property="id" column="lesson_id"></id>
            <result property="courseId" column="course_id"></result>
            <result property="sectionId" column="section_id"></result>
            <result property="theme" column="theme"></result>
            <result property="duration" column="duration"></result>
            <result property="isFree" column="is_free"></result>
            <result property="createTime" column="create_time"></result>
            <result property="updateTime" column="update_time"></result>
            <result property="isDel" column="is_del"></result>
            <result property="orderNum" column="order_num"></result>
            <result property="status" column="status"></result>
        </collection>
    </resultMap>
    
    
    <!--根据课程名查询章节信息及其关联的课时信息-->
    <select id="findSectionAndLessonByCourseId" parameterType="int" resultMap="sectionResultMap">
        SELECT cs.*, cl.id lesson_id, cl.course_id, cl.section_id, cl.theme,
               cl.duration, cl.is_free, cl.create_time, cl.update_time, cl.is_del,
               cl.order_num, cl.status
        FROM course_section cs LEFT JOIN course_lesson cl
        ON cs.id = cl.section_id
        WHERE cs.course_id = #{course_id}
        ORDER BY cs.`order_num`
    </select>
    
    <!--回显章节对应的课程信息-->
    <select id="findCourseByCourseId" parameterType="int"
            resultType="course">
        SELECT id,course_name FROM course WHERE id = #{courseId}
    </select>
    
    <!--新增章节信息-->
    <insert id="saveSection" parameterType="courseSection">
        INSERT INTO course_section (course_id, section_name, description, order_num,
                                    create_time, update_time, is_de, `status`)
        VALUES (#{courseId}, #{sectionName}, #{description}, #{orderNum}, #{createTime}, #{updateTime}, #{isDe}, #{status});
    </insert>
    
    <!--修改章节信息-->
    <update id="updateSection" parameterType="com.lagou.domain.CourseSection">
        UPDATE course_section
        <trim prefix="SET" suffixOverrides=",">
            <if test="sectionName != null and sectionName != ''">
                section_name = #{sectionName},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="orderNum != null and orderNum != '' or orderNum == 0">
                order_num = #{orderNum},
            </if>
            <if test="updateTime != null">
                update_time=#{updateTime}
            </if>
        </trim>
        <where>
            <if test="id != null and id != '' ">
                id = #{id}
            </if>
        </where>
    </update>
    
    <!--修改章节状态-->
    <update id="updateSectionStatus" parameterType="com.lagou.domain.CourseSection">
        UPDATE course_section set
        status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>
</mapper>