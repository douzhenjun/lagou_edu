<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lagou.dao.RoleMapper">
    
    <!--多条件查询角色列表-->
    <select id="findAllRole" parameterType="com.lagou.domain.Role" resultType="com.lagou.domain.Role">
        select * from roles
        <where>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
        </where>
    </select>
    
    <!--新增角色-->
    <insert id="saveRole" parameterType="role">
        insert into roles (code, name, description, created_time, 
                           updated_time, created_by, updated_by)
                values(#{code}, #{name}, #{description}, #{createdTime}, #{updatedTime}, #{createdBy}, #{updatedBy})
    </insert>
    
    <!--修改角色-->
    <update id="updateRole" parameterType="role">
        update roles
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null and name !=''">
                name = #{name},
            </if>
            <if test="code != null and code != ''">
                code = #{code},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
        </trim>
        where id = #{id}
    </update>
    
    <!--根据角色id查询关联的菜单id-->
    <select id="findAllMenuByRoleId" parameterType="int" resultType="menu">
        SELECT m.id FROM menu m, roles r, role_menu_relation rm
        WHERE m.id = rm.menu_id
          AND rm.role_id = r.id
          AND r.id = #{role_id};
    </select>
    
    <!--根据roleid删除表中的记录-->
    <delete id="deleteRoleContextMenu" parameterType="roleMenuVo">
        delete from role_menu_relation where role_id = #{roleId}
    </delete>
    
    <!--向表中新增记录-->
    <insert id="saveRoleMenuRelation" parameterType="roleMenuRelation">
        insert into role_menu_relation (menu_id, role_id, created_time, updated_time, created_by, updated_by)
        values(#{menuId}, #{roleId}, #{createdTime}, #{updatedTime}, #{createdBy}, #{updatedBy})
    </insert>
    
    <!--角色表删除记录-->
    <delete id="deleteRole" parameterType="int">
        delete from roles where id = #{roleId}
    </delete>
</mapper>